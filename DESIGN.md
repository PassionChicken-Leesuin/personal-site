---
version: 2
name: suin-blueprint
description: >
  이수인의 개인 홈페이지 디자인 시스템. 따뜻한 오프화이트 용지 위에 잉크 한 색으로
  그린 제도(製圖)를 은유로 삼는다. 첫 화면은 HELLO 한 단어뿐이고, 그 글자를 누르면
  용지가 넘어가듯 본문이 열린다. 배경에는 선으로만 그린 입체가 아주 느리게 돌아간다.
  논문·특허를 담은 연구자 포트폴리오이므로, 장식이 내용보다 커지지 않는 것이
  이 시스템의 첫 번째 제약이다.
provenance: >
  스페이싱 스케일과 문서 구조는 VoltAgent/awesome-design-md (MIT) 의
  design-md/claude/DESIGN.md 에서 가져왔다. 팔레트·타이포그래피·모션은 v1(구름과 나무)
  에서 전부 교체했다. 모션의 정량 상한은 nextlevelbuilder/ui-ux-pro-max-skill 의
  data/motion.csv 를 따랐다.
---

## 은유

**용지 위의 도면.**

색으로 강조하지 않는다. 위계는 세 가지로만 만든다 — 글자 크기, 자간, 괘선.
색을 하나 더 들이는 순간 이 은유가 깨지기 때문에, 액센트 컬러가 아예 없다.
상태 변화(hover·active)도 색이 아니라 농도와 자간으로 표현한다.

## 팔레트

라이트는 따뜻한 오프화이트 용지, 다크는 그것을 뒤집은 청사진이다.

| 역할 | 라이트 | 다크 | 쓰임 |
|---|---|---|---|
| `--canvas` | `#f2f0ea` | `#141311` | 용지 |
| `--surface-soft` | `#ebe8e0` | `#1c1b18` | 눌린 면(링크 행 hover) |
| `--hairline` | `#d3cfc3` | `#3a3831` | 실선 — 구조를 그린다 |
| `--hairline-soft` | `#e2dfd5` | `#292722` | 연선 — 작도선·방안지 |
| `--ink` | `#1c1b17` | `#f2f0e9` | 제목·강조·3D 선 |
| `--body` | `#4a4740` | `#c3bfb4` | 본문 |
| `--muted` | `#6b6659` | `#948e80` | 레이블·연도 |

대비(WCAG AA, `--canvas` 위):

| | 라이트 | 다크 |
|---|---|---|
| `--ink` | 15.12:1 | 16.28:1 |
| `--body` | 8.13:1 | 10.11:1 |
| `--muted` | 5.02:1 | 5.69:1 |

`--muted` 는 10–11px 로 쓰이지만 큰 텍스트 예외(3:1)에 기대지 않고 일반 기준
4.5:1 을 넘긴다. 작은 글씨야말로 여유가 필요하다.

## 타이포그래피

- **Jost** (`--font-display`) — 기하학적 그로테스크. 원이 정원에 가까워 제도 도구로
  그은 듯한 인상을 준다. 제목과 HELLO 에 쓰고, 굵기는 200–300 만 쓴다.
- **Inter** (`--font-sans`) — 본문. 작은 크기에서 흐트러지지 않는다.
- **한글** — 웹폰트를 얹지 않는다. 한글 웹폰트는 수백 KB라 정적 사이트의 이점을
  깎는다. Pretendard → Apple SD Gothic Neo → Malgun Gothic 순의 시스템 스택.

자간이 이 시스템의 주된 도구다.

| 용도 | 자간 |
|---|---|
| HELLO (게이트) | `0.42em` → hover `0.56em` |
| 이름 (h1, 대문자) | `0.1em` |
| `.label` (10–11px 대문자) | `0.2em` |
| 섹션 제목 | `0.01em` |

## 화면 단계

`Stage = "hello" | "home" | "work" | "awards" | "journey" | "social" | "contact"`

주소는 `/` 하나로 고정한다. 대신 전환마다 같은 URL 로 `pushState` 해서
히스토리 항목만 쌓는다 — 뒤로가기가 이전 화면으로, 끝내는 HELLO 로 돌아온다.
최초 항목에는 `state.entered` 가 없고, 그것이 곧 게이트의 표시다.

## 3D

`src/components/scene/` — 선으로만 그린다. 채운 면이 없으므로 **조명이 하나도 없다**.
색이 조명에 흔들리지 않아 CSS 토큰의 잉크색이 화면에 그대로 나온다.

- 정육면체 둘(서로 반대로 회전), 팔면체, 사면체, 원 둘, 사각틀 둘
- 공전 노드는 섹션 수만큼(다섯). 가장 높이 도는 노드도 상단 내비 상자 아래로
  지나가야 한다 — 상자는 항목이 늘면 길어진다
- 한 점에 모으지 않고 흩어 놓는다 — 뭉치면 도면이 아니라 실뭉치가 된다
- 회전은 한 바퀴에 1–3분. 어느 순간을 잘라도 정지된 도면처럼 보여야 한다
- 지오메트리는 모듈 수준에서 한 벌만 만들어 공유한다. 컴포넌트 안에서 만들고
  unmount 에 dispose 하면 StrictMode 이중 마운트가 이미 버려진 것을 그린다

씬은 단계마다 자리를 옮긴다: 게이트에서는 한가운데, 홈에서는 오른쪽,
섹션에서는 구석. 순간이동하지 않고 매 프레임 목표를 향해 감쇠하며 다가간다.

## 모션 상한

- 화면 전환 460ms / 게이트 퇴장 620ms / 스크롤 등장 700ms
- easing 은 `cubic-bezier(0.16, 1, 0.3, 1)` 하나로 통일
- `prefers-reduced-motion` 에서 게이트는 지연 없이 즉시 걷히고, 3D 는 초기 위상에
  멈춘 채로 보인다 — 사라지지 않는다. 정지된 도면은 그 자체로 완결이다

## 무너지지 않아야 하는 것

1. **WebGL 이 없으면 캔버스를 아예 만들지 않는다.** 글과 링크만으로 성립한다
2. **DOM 순서 = 탭 순서.** 게이트의 첫 탭 정거장은 HELLO 다
3. **캔버스는 `aria-hidden`.** 내비게이션은 상단 상자의 진짜 버튼이 맡는다
4. **가로 스크롤 금지.** 화면 전체를 쓰는 캔버스가 있는 레이아웃의 단골 사고다
5. **좁은 화면에서는 공전 노드 라벨을 끈다.** 상단 내비 상자와 겹친다
